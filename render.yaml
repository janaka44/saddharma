databases:
  - name: saddharma_org
    databaseName: saddharma_postgres
    user: db-admin

services:
  - type: web
    name: saddharma_org
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn mysite.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: saddharma_postgres
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4